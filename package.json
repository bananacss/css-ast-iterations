{
  "name": "css-ast-iterations",
  "description": "Provide a very simple API for complex iterations on the CSS abstract syntax tree (AST).",
  "version": "0.1.0",
  "author": {
    "name": "afonsopacifer",
    "email": "afonsopacifer@live.com"
  },
  "bugs": {
    "url": "https://github.com/afonsopacifer/css-ast-iterations/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/afonsopacifer/css-ast-iterations"
  },
  "keywords": [
    "css",
    "parser",
    "style",
    "stylesheets",
    "AST"
  ],
  "license": "MIT",
  "main": "src/index.js",
  "dependencies": {
    "css": "^2.2.1"
  },
  "scripts": {
    "test": "mocha test/index.js",
    "cover": "istanbul cover _mocha -- ./test/* --recursive",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls",
    "eslint": "eslint src/* src/core/* test/*"
  },
  "devDependencies": {
    "coveralls": "^2.11.12",
    "eslint": "^3.2.2",
    "istanbul": "^0.4.4",
    "mocha": "^3.0.0",
    "mocha-lcov-reporter": "^1.2.0"
  }
}
